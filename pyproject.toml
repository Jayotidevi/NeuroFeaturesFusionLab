[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "neurofeaturefusion"
version = "0.1.0"
description = "Multimodal EEG and audio fusion framework"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
